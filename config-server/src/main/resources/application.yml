spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        default-label: master
        git:
          uri: https://github.com/umitsayin/gelecegi-yazanlar-rentacar-microservice-config-server
server:
  port: 8888

spring:
    datasource:
        password: postgres
        url: jdbc:postgresql://localhost:5432/t1-inventory-service
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
            javax:
                persistence:
                    validation:
                        mode: none
